@{
    Layout = "~/Views/Shared/_LayoutPage2.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                <h2 class="panel-title">All Domain</h2>
            </header>
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover mb-none">
                        <thead>
                            <tr>
                                <th>Domain ID</th>
                                <th>Domain Name</th>
                                <th>Action</th>

                            </tr>
                        </thead>

                        <tbody>
                            @for (int i = 0; i < Model.Rows.Count; i++)
                            {
                                <tr>
                                    <td>@Model.Rows[i]["DomainID"]</td>
                                    <td>@Model.Rows[i]["DomainName"]</td>
                                    
                                    <td>
                                        <a href="/Admin/DomainEdit?SID=@Model.Rows[i]["DomainID"]">Edit</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </section>

    </div>
</div>
